@model Moo_Arvelous_Coders.Models.Farmer

@{
    ViewData["Title"] = "Farmer Details";
}

<link rel="stylesheet" href="~/css/site.css" />
<link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@700&family=Poppins:wght@300;600&display=swap" rel="stylesheet">

<div class="personal-account-container">
    <div class="herd-header" style="text-align:center;">
        <img src="~/images/Logo.png" alt="HerdWise Logo" class="logo-top" />
        <h1 class="form-title">FARMER DETAILS</h1>
        <p class="subtitle">View detailed information about this farmer profile.</p>
        <a class="menu-button" href="/">MENU</a>
    </div>

    <div class="account-container">
        <div class="account-box">
            <h3 class="form-heading">Personal Information</h3>
            <dl>
                <dt>First Name:</dt>
                <dd>@Model.FirstName</dd>

                <dt>Last Name:</dt>
                <dd>@Model.LastName</dd>

                <dt>ID Number:</dt>
                <dd>@Model.Idnumber</dd>

                <dt>Phone Number:</dt>
                <dd>@Model.PhoneNumber</dd>

                <dt>Email Address:</dt>
                <dd>@Model.EmailAddress</dd>

                <dt>Location:</dt>
                <dd>@Model.Location</dd>
            </dl>

            <div class="button-row">
                <a asp-action="Edit" asp-route-id="@Model.FarmerId" class="submit-btn">EDIT</a>
                <a asp-action="Index" class="clear-btn">BACK TO LIST</a>
            </div>
        </div>
    </div>

    <footer class="footer-text">
        <p>Life isn't perfect, but with HerdWise your farm can be.</p>
    </footer>
</div>
