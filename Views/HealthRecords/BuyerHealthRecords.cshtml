@model IEnumerable<Moo_Arvelous_Coders.Models.CattleHealthRecord>

<link rel="stylesheet" href="~/css/site.css" />

<div class="page-container">
    <div class="header-section">
        <img src="~/images/Logo.png" alt="Logo" class="logo-top" />
        <h1 class="welcome-title" style="max-width: 400px; margin: 0 auto;">Cattle Health Records</h1>
       
    </div>

    <div class="main-cattle-container" style="max-width: 600px; margin: 0 auto;">
        <div class="cattle-form cattle-details">
            <h3 class="form-headingC" style="max-width: 400px;">Health Records for @ViewBag.CattleName</h3>

            @if (!Model.Any())
            {
                <p class="text-muted">No health records found for this cattle.</p>
            }
            else
            {
                <table class="styled-table" style="width: 100%; margin-top: 10px;">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Treatment Type</th>
                            <th>Details</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var record in Model)
                        {
                            <tr>
                                <td>@record.RecordDate.ToString("yyyy-MM-dd")</td>
                                <td>@record.TreatmentType</td>
                                <td>@record.Details</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            
        </div>
    </div>

    <footer class="footer-text">
        <p>Life isn't perfect, but with HerdWise your farm can be.</p>
    </footer>
</div>
